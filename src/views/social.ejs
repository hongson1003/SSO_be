<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>

</head>
<script>

    $(document).ready(() => {
        const redirectURL = localStorage.getItem('clientURL');
        localStorage.removeItem('clientURL');
        var ssoToken = $('#ssoToken').val();

        window.location.href = `${redirectURL}/codeToken?sso=${ssoToken}`;

    })

</script>

<body>
    <input id="ssoToken" hidden value="<%= ssoToken %>" / </body>

</html>